Users can:
- Register with email and password
- Login
- Reset password

Session must persist across app restarts.

Bookmark rule:
- If user is logged in → allow bookmarking
- If not → redirect to login screen
