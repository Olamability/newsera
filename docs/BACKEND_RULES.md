Rules:

1. Only sources with status = 'approved' are used in RSS ingestion

2. articles.url must be unique to avoid duplicates

3. bookmarks.user_id must always exist

4. Categories must exist before assigning to articles or sources

5. No full article should be displayed in app (snippet only)
